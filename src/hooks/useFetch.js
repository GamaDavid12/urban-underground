import { useState, useEffect, useCallback } from 'react';

export const useFetch = (url) => {
  // ... (código del hook genérico) ...
  // Asegúrate de que este hook maneje el token de autenticación (JWT) en los headers:
  // headers: { 
  //   'Authorization': `Bearer ${token}` 
  // }
};